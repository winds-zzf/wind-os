####################
# 自动化编译配置文件 #
####################


# 将./boot/目录下的.asm编译成.o文件
%.o : $(CCBUILDPATH)%.asm
	$(ASM) $(ASMFLAGS) -o $@ $<
	@echo 'ASM -[M] 正在构建'$<

# 将./boot/目录下的.c编译成.o文件
%.o : $(CCBUILDPATH)%.c
	$(CC) $(GCCFLAGS) -o $@ $<
	@echo 'GCC -[M] 正在构建'$<